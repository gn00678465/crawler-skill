[project]
name = "crawler-skill"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "firecrawl-py>=2.0",
    "httpx>=0.27",
    "scrapling>=0.2",
    "html2text>=2024.2.26",
    "pytest>=8.0",
    "pytest-mock>=3.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
